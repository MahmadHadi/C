// Write a programe to findout how many days user given month has 
#include<stdio.h>
void main()
{
    int a;
    
    printf("Enter number of month: ");
    scanf("%d",&a);
    
    if(a==1 || a==3 || a==)
    
}